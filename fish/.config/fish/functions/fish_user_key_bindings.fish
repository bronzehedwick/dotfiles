function fish_user_key_bindings
  bind \cr history-prefix-search-backward
  bind \cf forward-bigword
  bind \cb backward-bigword
  bind \cd backward-kill-bigword
  bind \ed backward-kill-word
  bind \el downcase-word
end

